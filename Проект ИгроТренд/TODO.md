# TODO - ИгроТренд Platform

> **Примечание:** все пункты из этого списка были импортированы в GitHub issues (`FijiQ/igrotrend`) 24 фев 2026. Слежение ведётся через соответствующие задачи.


## 🔴 КРИТИЧНО (Перед коммитом в GitHub)

### Безопасность
- Удалить `Данные для девелопера.txt` из проекта (сделано).
- Создать `.env.example` с шаблоном переменных окружения (существует, проверено). ✅
- Создать/проверить `.gitignore` (существует, убеждён что покрывает все).
- Сгенерировать сильный `JWT_SECRET` (не хранить в репозитории).
- Убедиться, что `.env` не попадает в git.
- Переписать `DEPLOY.md`: убрать примеры слабых паролей. ✅ (добавлена заметка про безопасный `JWT_SECRET` и миграции).

### Очистка проекта
- Удалить `Новая таблица.xlsx` (если появится).
- Проверить, что все demo-данные очищены (по worklog).
- Удалить упоминания "Z.ai" из `README.md`.
- Обновить `README.md` под реальное описание проекта ИгроТренд. ✅ (текущая версия отражает ИгроТренд, ‘Z.ai’ удалён).

## 🟠 ВЫСОКИЙ ПРИОРИТЕТ (Перед production)

### API
- Добавить healthcheck endpoint (`/api/health`). ✅ Реализован.


### База данных
- Настроить Prisma migrations вместо `db:push`. ✅ (первичная миграция создана и закоммичена)
  - `prisma migrate dev` для разработки.
  - `prisma migrate deploy` для production.
- Добавить таблицу для refresh-токенов (сделано).
- (Документация обновлена, README/DEPLOY.md содержат инструкции.)
- Добавить инструкции в `package.json` скрипты (есть `db:migrate`/`db:deploy`).
- Выбрать production БД (PostgreSQL/MySQL/PlanetScale).
- Создать миграционную стратегию с rollback.
- Настроить автоматические бэкапы БД.
- Добавить индексы в критичные таблицы Prisma.

### TypeScript
- Включить `"noImplicitAny": true` в `tsconfig.json`.
- Исправить все ошибки типизации.
- Добавить `"strictNullChecks": true`.
- Настроить ESLint с строгими правилами.

### Аутентификация
- Настроить реальную отправку email (SMTP). ✅ реализация через nodemailer и `src/lib/email.ts`.
- Добавить rate limiting для API эндпоинтов. ✅ простая in-memory функция в `src/lib/rate-limit.ts` (примеры в login/register/verify).
- Реализовать refresh tokens. ✅ добавлен `RefreshToken` модель, endpoints login/refresh/logout + helper `src/lib/auth.ts`.
- Добавить 2FA (опционально). ✅ модель расширена, новые эндпоинты.
- Настроить CORS политики. ✅ добавлен middleware `src/middleware.ts`; настраивается через `ALLOWED_ORIGINS`.

## 🟡 СРЕДНИЙ ПРИОРИТЕТ (После запуска)

### DevOps
- Создать `Dockerfile` (шаблон есть в `DEPLOY.md`).
- Создать `docker-compose.yml`.
- Настроить GitHub Actions:
  - Линтинг и проверка типов
  - Автоматический билд
  - Деплой на staging/production
- Настроить ротацию логов (убрать `tee` из npm scripts).
- Добавить healthcheck endpoint (`/api/health`).

### Мониторинг
- Интегрировать Sentry для отслеживания ошибок.
- Настроить логирование (Winston/Pino).
- Добавить метрики (время ответа API, RPS).
- Настроить алерты для критичных ошибок.
- Создать dashboard для мониторинга.

### Тестирование
- Настроить Jest для unit-тестов.
- Добавить Playwright для E2E тестов.
- Написать тесты для API эндпоинтов.
- Добавить тесты для критичных компонентов.
- Настроить code coverage (минимум 70%).

## 🟢 НИЗКИЙ ПРИОРИТЕТ (Оптимизация)

### Performance
- Настроить ISR/SSG для статичных страниц.
- Оптимизировать bundle size (анализ с `@next/bundle-analyzer`).
- Добавить image optimization (уже есть Sharp).
- Настроить CDN для статики.
- Включить HTTP/2 Server Push.

### UX/UI
- Добавить skeleton loaders.
- Реализовать infinite scroll для лент.
- Добавить PWA функционал (manifest, service worker).
- Оптимизировать анимации (reduce motion).
- Добавить кастомные 404/500 страницы.

### Документация
- Создать `CONTRIBUTING.md`.
- Добавить `LICENSE` файл.
- Написать API документацию (Swagger/OpenAPI).
- Создать Component Storybook.
- Добавить архитектурную диаграмму.

### Функционал
- Реализовать уведомления (in-app + push).
- Добавить поиск по платформе.
- Создать систему достижений.
- Реализовать чат между пользователями.
- Добавить интеграцию с Discord/Steam.

### 📦 Зависимости (проверить актуальность)
- Проверить совместимость React 19 и библиотек.
- Обновить все зависимости до последних патч-версий.
- Проверить security уязвимости (`bun audit`).
- Настроить Dependabot для автообновлений.

### 🎯 GitHub Setup
- Создать репозиторий `igrotrend` или `igrotrend-platform`.
- Настроить branch protection для `main`.
- Создать шаблоны для Issues и PR.
- Настроить GitHub Projects.
- Добавить Topics: `nextjs`, `gaming`, `social-network`, `prisma`, `typescript`.
- Создать Issues по всем пунктам TOD
- Добавить labels: security, bug, enhancement, documentation.
- Создать milestones: v1.0, v1.1, v2.0.
- Назначить приоритеты (P0-P3).

### 📊 Метрики успеха
- Performance: Lighthouse > 90, TTFB < 200ms, FCP < 1.5s, CLS < 0.1.
- Code Quality: TypeScript без any, ESLint 0 ошибок, Coverage > 70%, Bundle < 500KB.
- Security: нет критичных уязвимостей, HTTPS enforced, заголовки, rate limiting.

### 🎮 Roadmap (фичи)
- v1.0 (MVP): регистрация, профили, лента, клубы, команды, Dev Panel, мультиязычность.
- v1.1: реальная БД, email уведомления, поиск, модерация.
- v2.0: мобильное приложение, live streaming, турнирная система, монетизация, AI модерация.

> Дата создания: 2026-02-24
> Статус: 🚧 В разработке
> Приоритет: Начать с 🔴 КРИТИЧНО раздела
